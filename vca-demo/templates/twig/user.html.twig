{# templates/lucky/number.html.twig #}
{% extends "base.html.twig" %}

    {% block title %}
        Random Number
    {% endblock %}

    {% block stylesheets %}
    {{ parent() }}
    {% endblock %}

    {% block body %}


        {{ include('request/include_info.html.twig') }}

        <p>The Google tracking code is: {{ ga_tracking }}</p>
        <a href="{{ path('homepage') }}">Homepage</a>
          <a href="{{ url('homepage') }}">Homepage_URL</a>
        <br>
        {% set route_name = app.current_route %}
        {% set route_parameters = app.current_route_parameters %}
        {{ params.name }}
        <br>
        {{ params.routeParameters }}

        <br>  configration app
        <br>
        {{ app.current_route }}
        {% for key, val in route_parameters %}
            <br>
            {{ val }}
            <br>
        {% endfor %}

        <div id="sidebar">

                {{ render(controller(
                    'App\\Controller\\TemplateController::userArticles')) }}
            </div>
    {% endblock %}
     {% block footer %}
         <h4>All copyright @2023</h4>
     {% endblock %}
